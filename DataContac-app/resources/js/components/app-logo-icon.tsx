import { DataContacIcon } from '@/components/data-contact-icon';
import type { SVGProps } from 'react';

export default function AppLogoIcon(props: SVGProps<SVGSVGElement>) {
    return <DataContacIcon color="currentColor" {...props} />;
}